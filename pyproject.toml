[project]
name = "listino"
version = "0.1.0"
description = "Calcolo di importi di listino — repo di prova per la fucina"
requires-python = ">=3.11"

[project.optional-dependencies]
dev = ["pytest>=8.0"]

[tool.pytest.ini_options]
testpaths = ["tests"]
